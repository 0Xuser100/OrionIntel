MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# RabbitMQ (broker) — must match CELERY_BROKER_URL in src/.env
RABBITMQ_DEFAULT_USER=
RABBITMQ_DEFAULT_PASS=
RABBITMQ_DEFAULT_VHOST=

# Redis (result backend) — must match CELERY_RESULT_BACKEND in src/.env
REDIS_PASSWORD=

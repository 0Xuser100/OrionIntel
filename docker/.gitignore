# deploy env files — keep only the .env.example.* templates
env/.env*
!env/.env.example.*

# generated from alembic.example.ini
orionintel/alembic.ini

# local-dev env files (docker/local/)
local/.env

# compose-level interpolation file (holds REDIS_PASSWORD); .env.example is committed
/.env
